{
	"name": "pg-optimizer",
	"version": "1.0.0",
	"description": "PostgreSQL 性能参数优化工具",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"preview": "vite preview",
		"deploy": "npm run build && npx wrangler pages deploy dist",
		"pages:deploy": "npx wrangler pages deploy dist"
	},
	"dependencies": {
		"vue": "^3.4.21"
	},
	"devDependencies": {
		"@vitejs/plugin-vue": "^5.0.4",
		"vite": "^5.1.4"
	}
}